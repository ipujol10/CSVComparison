# CSVComparison
Compare 2 CSV files and return the different cells (in an Excel format).

If you are comparing Excel files you first need to export the (individual)
Worksheets you want to compare into CSV files.
